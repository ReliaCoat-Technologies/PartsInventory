<UserControl x:Class="reliacoatInventory.Views.InventoryLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:ViewModels="clr-namespace:reliacoatInventory.ViewModels"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             mc:Ignorable="d"
             DataContext="{dxmvvm:ViewModelSource Type=ViewModels:InventoryLogViewModel}">
    <DockPanel>

        <UniformGrid DockPanel.Dock="Top" Margin="5" Columns="2">
            <TextBlock Text="Start Date" Margin="5"/>
            <TextBlock Text="End Date" Margin="5"/>
            <dxe:DateEdit AllowNullInput="True" NullText="None" EditValue="{Binding startDate}" Margin="5">
                <dxmvvm:Interaction.Triggers>
                    <dxmvvm:EventToCommand EventName="EditValueChanged" Command="{Binding refreshUICommand}"/>
                </dxmvvm:Interaction.Triggers>
            </dxe:DateEdit>
            <dxe:DateEdit AllowNullInput="True" NullText="None" EditValue="{Binding endDate}" Margin="5">
                <dxmvvm:Interaction.Triggers>
                    <dxmvvm:EventToCommand EventName="EditValueChanged" Command="{Binding refreshUICommand}"/>
                </dxmvvm:Interaction.Triggers>
            </dxe:DateEdit>
        </UniformGrid>

        <UniformGrid DockPanel.Dock="Bottom" Columns="3" Margin="5">
            <Button Content="Clear Filters" Margin="5"/>
            <Button Content="Export to CSV" Margin="5"/>
            <Button Content="Expor to PDF" Margin="5"/>
        </UniformGrid>

        <UniformGrid DockPanel.Dock="Top" Margin="5" Columns="3">
            <TextBlock Text="Item" Margin="5"/>
            <TextBlock Text="User" Margin="5"/>
            <TextBlock Text="Account" Margin="5"/>
            <ComboBox ItemsSource="{Binding itemList}" SelectedItem="{Binding item, Mode=OneWayToSource}" Margin="5">
                <dxmvvm:Interaction.Triggers>
                    <dxmvvm:EventToCommand EventName="DropDownClosed" Command="{Binding refreshUICommand}"/>
                </dxmvvm:Interaction.Triggers>
            </ComboBox>
            <ComboBox ItemsSource="{Binding userList}" SelectedItem="{Binding user, Mode=OneWayToSource}" Margin="5">
                <dxmvvm:Interaction.Triggers>
                    <dxmvvm:EventToCommand EventName="DropDownClosed" Command="{Binding refreshUICommand}"/>
                </dxmvvm:Interaction.Triggers>
            </ComboBox>
            <ComboBox ItemsSource="{Binding accountList}" SelectedItem="{Binding account, Mode=OneWayToSource}" Margin="5">
                <dxmvvm:Interaction.Triggers>
                    <dxmvvm:EventToCommand EventName="DropDownClosed" Command="{Binding refreshUICommand}"/>
                </dxmvvm:Interaction.Triggers>
            </ComboBox>
        </UniformGrid>

        <dxg:GridControl SelectionMode="Row" ItemsSource="{Binding inventoryLogList}" Margin="5">
            <dxg:GridControl.View>
                <dxg:TableView AutoWidth="True" AllowPerPixelScrolling="True" AllowEditing="False" ShowGroupPanel="False"/>
            </dxg:GridControl.View>
            <dxg:GridControl.Columns>

                <dxg:GridColumn FieldName="dateTime" Header="Date - Time" Width="100" HorizontalHeaderContentAlignment="Center">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings HorizontalContentAlignment="Center"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

                <dxg:GridColumn FieldName="user" Header="User" Width="100" HorizontalHeaderContentAlignment="Center">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings HorizontalContentAlignment="Center"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

                <dxg:GridColumn FieldName="account" Header="Account" Width="70" HorizontalHeaderContentAlignment="Center">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings HorizontalContentAlignment="Center"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

                <dxg:GridColumn FieldName="itemID" Header="Item ID" Width="70" HorizontalHeaderContentAlignment="Center">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings HorizontalContentAlignment="Center"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

                <dxg:GridColumn FieldName="description" Header="Description" Width="400" HorizontalHeaderContentAlignment="Center">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings HorizontalContentAlignment="Center"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

                <dxg:GridColumn FieldName="quantityBefore" Header="Before" Width="50" HorizontalHeaderContentAlignment="Center">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings HorizontalContentAlignment="Center"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

                <dxg:GridColumn FieldName="quantityChanged" Header="+/-" Width="50" HorizontalHeaderContentAlignment="Center">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings HorizontalContentAlignment="Center"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

                <dxg:GridColumn FieldName="quantityAfter" Header="After" Width="50" HorizontalHeaderContentAlignment="Center">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings HorizontalContentAlignment="Center"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

            </dxg:GridControl.Columns>
        </dxg:GridControl>
    </DockPanel>
</UserControl>
